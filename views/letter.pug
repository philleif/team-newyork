extends simple

block content
  if (letter && user)
    section
      .container.grid-md
        +postcard(letter, user.representatives.councilMember, false)
  else
    center
      h6 You've already sent this letter!
      p We'll send you an alert once there's more news.
